# API Server

The client application that interacts with the Hyperledger Fabric network via the Gateway SDK.

## Features

- RESTful endpoints to Invoke and Query chaincode.
- Manages user identities and wallets.
